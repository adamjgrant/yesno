<%= form_for(@issue, :html => {:class => "default_form"}) do |f| %>
  <% if @issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@issue.errors.count, "error") %> prohibited this issue from being saved:</h2>

      <ul>
      <% @issue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :news_title%>
    <%= f.text_field :news_title %>
  </div>
  <div class="field">
    <%= f.label :news_link %>
    <%= f.text_field :news_link %>
  </div>
  <div class="field">
    <%= f.label :news_source %>
    <%= f.text_field :news_source %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
    <p>Use an affirmative title, not a question. E.g. "John is good at math"</p>
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
    <p>Now pose the title as a question with some snark.</p>
  </div>
  <div class="field">
    <%= f.label :image%>
    <%= f.text_field :image%>
    <p>Full url</p>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
